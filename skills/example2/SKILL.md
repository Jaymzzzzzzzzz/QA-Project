---
name: example2 - replace with relevant name that you want for this skill
description: Example skill — replace with domain-specific instructions for your agent.
---

This is a second example skill showing computation guidance.

Replace this with domain-specific formulas, such as:

- Percent change: `((new - old) / old) * 100`
- CAGR: `(end / start) ** (1/n) - 1`
- Theil index: `(1/n) * sum((xi / mean) * ln(xi / mean))`
- Geometric mean: `(x1 * x2 * ... * xn) ** (1/n)`
